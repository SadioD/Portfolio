{% extends "base.html.twig" %}
    {# Page CSS AND TITLE --------------------------------------------------------------------------------------------------------- #}
    {% block title %}
        Sadio DIALLO
    {% endblock title %}
    {# ---------------------------------------------------------------------------------------------------------------------------- #}
    {# Page Body ------------------------------------------------------------------------------------------------------------------ #}
    {% block body %}
        <header>
            <nav class="navbar navbar-default navbar-inverse">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
                            aria-expanded="false">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <a class="navbar-brand" href="#">SADIO</a>
                    </div>
                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav">
                            <li>
                                <a href="{{ path('sadio_auth_security_logout') }}" class="navbar-link menuLinks">Logout</a>
                            </li>
                            <li>
                                <a href="{{ path('sadio_auth_security_profile') }}" class="navbar-link menuLinks">Inbox</a>
                            </li>
                            <li>
                                <a href="{{ path('sadio_auth_security_outbox') }}" class="navbar-link menuLinks">Outbox</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </header>
    
        <div class="container">
            <section class="row">
                <div class="col-xs-8">
                    <h1>Welcome Home</h1>
                    <p>From Here, You'll be able to manage your account...</p>
                </div>
            </section>
            {# Bundle body #}
            {% block admin_body %}
            {% endblock admin_body %}
        </div>
    {% endblock body %}